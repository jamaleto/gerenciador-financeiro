"use client";import {FormEvent,useState} from "react";import {LockKeyhole} from "lucide-react";
export default function Login(){const [error,setError]=useState(""),[loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const data=new FormData(e.currentTarget);const r=await fetch("/api/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({password:data.get("password")})});const j=await r.json();if(!r.ok){setError(j.error);setLoading(false);return}location.href="/"}return <main className="login"><form onSubmit={submit}><div className="login-logo">F</div><p>FLUXO EVENTOS</p><h1>Acesso financeiro</h1><span>Entre para consultar os dados da empresa e dos eventos.</span><label>Senha de acesso<input name="password" type="password" required autoComplete="current-password"/></label>{error&&<b className="login-error">{error}</b>}<button disabled={loading}><LockKeyhole/>{loading?"Entrando…":"Entrar no sistema"}</button></form></main>}